{
  "name": "ocorrencia-online",
  "version": "1.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm ci --prefix backend && npm ci --prefix frontend",
    "env:create": "node scripts/create-env.js",
    "env:check": "npm run env:check --prefix backend",
    "db:setup": "npm run db:setup --prefix backend",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm start --prefix frontend",
    "check": "npm run check --prefix backend && npm run build --prefix frontend"
  }
}
